# Homebrew Tap for Todo App

Install Todo App via Homebrew:

```bash
brew tap lilfourn/todoapp
brew install --cask todo-app
```

## Updating

```bash
brew upgrade todo-app
```

## Uninstalling

```bash
brew uninstall --cask todo-app

# Remove all data
brew uninstall --cask --zap todo-app
```
